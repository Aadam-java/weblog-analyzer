/**
 * 1. 6 pm is the busiest hour
 * 2. Person people[];
 * 3. boolean vacant[];
 * 4. square brakets were used 3 times with the hour counts variable
 * 5. int[] counts;
 * boolean[5000] occupied
 * 6.readings = new double[60];
 * urls = new double[60];
 * machines = TicketMachine[5];
 * 7. 20 string declarations
 * 8. prices = new double[50];
 * 9. because when we add equals it goes past the array limit since it would
 * be 25 when the limit is 24
 * 11. * @param marks An array of mark values.
 * @param mean The mean (average) mark.
    public void printGreater(double[] marks, double mean) {
    for (int index = 0; index < marks.length; index++) {
        if (marks[index] > mean) {
            System.out.println(marks[index]);
        }
    }
}
 *
 */